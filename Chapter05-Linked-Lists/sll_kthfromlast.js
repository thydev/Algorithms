//Return the k-th node from the last

function kthfromthelast(num){
    //set runner ==> the return node
    //set knode the num node from the runner
    //loop through the list unitl knode.next null
    //return runner.value
}